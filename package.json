{
  "name": "explinks-electron-sdk",
  "version": "1.0.0",
  "description": "ExpLinks SDK for Electron - Access thousands of cloud APIs from your desktop application",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "tsc",
    "dev": "tsc && electron .",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "explinks",
    "electron",
    "sdk",
    "api",
    "cloud"
  ],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
